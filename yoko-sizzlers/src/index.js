import React from 'react';
import ReactDOM from 'react-dom/client';
import YokoSizzlersApp from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <YokoSizzlersApp />
  </React.StrictMode>
);
